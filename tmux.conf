# Change default prefix key to C-a, similar to screen
unbind-key C-b
set-option -g prefix C-a

# Enable 24-bit color support
set-option -ga terminal-overrides ",xterm-termite:Tc"

# Start window indexing at one
set-option -g base-index 1

# Use vi-style key bindings in the status line, and copy/choice modes
set-option -g status-keys vi
set-window-option -g mode-keys vi

# Large scrollback history
set-option -g history-limit 10000

# Xterm Keys on
set-window-option -g xterm-keys on

# Set 256 colors
set -g default-terminal "tmux"

# Set escape time to zero
set -sg escape-time 0

# move between panes with vim-like motions
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Synchronizing panes
bind-key y set-window-option synchronize-panes

# SSH to Host
bind-key S command-prompt -p ssh: "new-window -n %1 'ssh %1'"

# Split windows
unbind-key %
bind-key | split-window -h -c "#{pane_current_path}"
unbind-key '"'
bind-key - split-window -v -c "#{pane_current_path}"

# Mouse mode
set -g mouse on

# Tmux Scrolling
bind -n WheelUpPane   select-pane -t= \; copy-mode -e \; send-keys -M
bind -n WheelDownPane select-pane -t= \;                 send-keys -M

# Tmux theme
set -g status-bg "colour8"
set -g message-command-fg "colour0"
set -g status-justify "left"
set -g status-left-length "100"
set -g status "on"
set -g pane-active-border-fg "colour8"
set -g message-bg "colour4"
set -g status-right-length "100"
set -g status-right-attr "none"
set -g message-fg "colour0"
set -g message-command-bg "colour4"
set -g status-attr "none"
set -g pane-border-fg "colour8"
set -g status-left-attr "none"
setw -g window-status-fg "colour0"
setw -g window-status-attr "none"
setw -g window-status-activity-bg "colour8"
setw -g window-status-activity-attr "none"
setw -g window-status-activity-fg "colour6"
setw -g window-status-separator ""
setw -g window-status-bg "colour8"
set -g status-left "#[fg=colour0,bg=colour6,nobold] #S #[fg=colour6,bg=colour8,nobold,nounderscore,noitalics]"
set -g status-right "#[fg=colour4,bg=colour8,nobold,nounderscore,noitalics]#[fg=colour0,bg=colour4] %Y-%m-%d %l:%M %p #[fg=colour6,bg=colour4,nobold,nounderscore,noitalics]#[fg=colour0,bg=colour6] #h "
setw -g window-status-format "#[fg=colour7,bg=colour8] #I:#[fg=colour7,bg=colour8] #W "
setw -g window-status-current-format "#[fg=colour8,bg=colour4,nobold,nounderscore,noitalics]#[fg=colour0,bg=colour4] #I:#[fg=colour0,bg=colour4] #W #[fg=colour4,bg=colour8,nobold,nounderscore,noitalics]"
